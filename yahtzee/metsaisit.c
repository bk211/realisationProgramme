#include "score.h"
#include "mecanisme.h"

void saisit(Player p){
    int dice = 0;
    if(dice =check_brelan(p->dices) >0){
        // 1 affiche score gagne par ce brealan de de [dice]
        // 2 demande si veux ecrire ce score dans le tableau final 
        // si oui, p->tabscorefinale[brelan] = dice * coefficient score
        // return ; fin de fct 
        //sinon continuer les autres test
    if(dice = check_full> 0){
        //meme chose pour les autre combinaison, mais aussi la partie superieur
    }
    }
}






int main(int argc, char const *argv[])
{
    /* code */
    return 0;
}

